<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title> Products list</title>
    @vite(['resources/css/app.css'])

</head>

<body>

    <header class="bg-gray-100 border-b border-b-gray-300">
        <div class="container mx-auto flex justify-between items-center h-16">
            <div>
                <a href="">Products tracker</a>
            </div>
            <div>
                <nav>
                    <ul class="flex space-x-4">
                        @if(auth.user)
                        {{ auth.user.fullName }}
                        @else
                        <li>
                            <a href="/login"> Login </a>
                        </li>
                        @end

                        <li>
                            <a href="/categories/create" class="underline" up-follow>Create category</a>
                        </li>
                        <li>
                            <a href="/expenses/create" class="underline" up-follow> Create product </a>
                        </li>
                    </ul>
                </nav>
            </div>
        </div>
    </header>

    <main class="py-24">
        <div class="container mx-auto flex">
            <div class="w-64">
                <aside class="h-full border-r border-r-gray-300">
                    <h3 class="text-3xl font-bold underline">Categories</h3>
                </aside>
            </div>

            <div class="pl-16">
                <h3 class="text-3xl font-bold underline">Products</h3>
            </div>
        </div>
    </main>


</body>

</html>